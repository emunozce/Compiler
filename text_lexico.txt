main {
    int x, y;
    double z;
    x=33-3;
    x = 5;
    y = 10;
    z = x + y * 2.5;

    if x < y then
        cout x;
    else
        cout y;
    end

    while x < 20 do
        x = x + 1;
    end

    do
        y = y - 1;
    while y > 0
    end

    cin z;
}
